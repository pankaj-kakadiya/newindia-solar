# Step 21 — Transaction & Communication Automation V2

This release adds database-backed communication rules/templates/outbox, Razorpay checkout and signature verification, Shiprocket shipment creation/tracking, payment reminders, and signed provider webhook processing. Provider secrets remain server-only and all transactional rules are disabled by default until the matching integration is configured and approved.
