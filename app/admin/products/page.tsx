'use client'
import {useEffect,useState} from 'react';import {supabase} from '../../../lib/supabase'
export default function Products(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{supabase.from('products').select('id,name,slug,product_type,status,base_price,gst_rate,stock_qty,categories(name)').order('created_at',{ascending:false}).limit(250).then(({data})=>setRows(data||[]))},[]);return <><div className="adminPageHead"><div><span className="adminEyebrow">CATALOGUE</span><h1>Products</h1><p>Ready products sold through the storefront.</p></div></div><section className="adminPanel"><div className="adminTableWrap"><table className="adminTable"><thead><tr><th>Product</th><th>Category</th><th>Type</th><th>Base Price</th><th>Stock</th><th>Status</th></tr></thead><tbody>{rows.length?rows.map(r=><tr key={r.id}><td><b>{r.name}</b><span className="tableSub">/{r.slug}</span></td><td>{r.categories?.name||'—'}</td><td>{r.product_type}</td><td>₹{Number(r.base_price||0).toLocaleString('en-IN')}</td><td>{r.stock_qty||0}</td><td><span className={`statusPill status-${r.status}`}>{r.status}</span></td></tr>):<tr><td className="emptyCell" colSpan={6}>No products added yet.</td></tr>}</tbody></table></div></section></>}
