'use client'
import {useEffect,useState} from 'react'
import {supabase} from '../../../lib/supabase'
export default function Components(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{supabase.from('components').select('id,category,brand,component_name,model,voltage,ampere,poles,selling_price,stock_qty,is_active').order('component_name').limit(300).then(({data})=>setRows(data||[]))},[]);return <><div className="adminPageHead"><div><span className="adminEyebrow">CATALOGUE MASTER</span><h1>Components</h1><p>Protection, wiring and assembly parts used by the configurator.</p></div></div><section className="adminPanel"><div className="adminTableWrap"><table className="adminTable"><thead><tr><th>Component</th><th>Category</th><th>Spec</th><th>Sell Price</th><th>Stock</th><th>Active</th></tr></thead><tbody>{rows.length?rows.map(r=><tr key={r.id}><td><b>{r.component_name}</b><span className="tableSub">{r.brand||''} {r.model||''}</span></td><td>{r.category}</td><td>{[r.voltage,r.ampere,r.poles&&`${r.poles}P`].filter(Boolean).join(' • ')||'—'}</td><td>₹{Number(r.selling_price||0).toLocaleString('en-IN')}</td><td>{r.stock_qty||0}</td><td>{r.is_active?'Yes':'No'}</td></tr>):<tr><td className="emptyCell" colSpan={6}>No components entered yet.</td></tr>}</tbody></table></div></section></>}
