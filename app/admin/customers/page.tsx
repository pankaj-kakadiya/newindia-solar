'use client'
import {useEffect,useState} from 'react';import {supabase} from '../../../lib/supabase'
export default function Customers(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{supabase.from('profiles').select('id,full_name,phone,company_name,gstin,role,created_at').order('created_at',{ascending:false}).limit(300).then(({data})=>setRows(data||[]))},[]);return <><div className="adminPageHead"><div><span className="adminEyebrow">CRM</span><h1>Customers</h1><p>Customer and business account records.</p></div></div><section className="adminPanel"><div className="adminTableWrap"><table className="adminTable"><thead><tr><th>Name</th><th>Company</th><th>Phone</th><th>GSTIN</th><th>Role</th><th>Joined</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.full_name||'Customer'}</td><td>{r.company_name||'—'}</td><td>{r.phone||'—'}</td><td>{r.gstin||'—'}</td><td>{r.role}</td><td>{new Date(r.created_at).toLocaleDateString('en-IN')}</td></tr>)}</tbody></table></div></section></>}
