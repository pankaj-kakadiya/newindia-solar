'use client'
import {FormEvent,useEffect,useState} from 'react';import {useRouter} from 'next/navigation';import {ShieldCheck} from 'lucide-react';import {supabase} from '../../../lib/supabase'
export default function Login(){const r=useRouter();const [msg,setMsg]=useState('');const [busy,setBusy]=useState(false);useEffect(()=>{supabase.auth.getUser().then(({data})=>{if(data.user)r.replace('/admin')})},[r]);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setMsg('');const f=new FormData(e.currentTarget);const {error}=await supabase.auth.signInWithPassword({email:String(f.get('email')),password:String(f.get('password'))});setBusy(false);if(error){setMsg(error.message);return}r.replace('/admin');r.refresh()}return <main className="adminLoginPage"><div className="adminLoginCard"><div className="adminLoginIcon"><ShieldCheck/></div><span className="adminEyebrow">NEW INDIA SOLAR</span><h1>Admin Control Center</h1><p>Authorized operations access only.</p><form onSubmit={submit}><label>Email<input name="email" type="email" required autoComplete="email"/></label><label>Password<input name="password" type="password" required autoComplete="current-password"/></label>{msg&&<div className="adminError">{msg}</div>}<button disabled={busy}>{busy?'Signing in…':'Sign in securely'}</button></form><small>Access requires an authenticated Supabase account with <b>admin</b> or <b>staff</b> role.</small></div></main>}
